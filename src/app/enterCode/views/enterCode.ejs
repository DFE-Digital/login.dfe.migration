<% if (locals.validationFailed && !locals.validationMessages.username && !locals.validationMessages.password) { %>
<div class="notification notification-error" tabindex="-1">
    <h1 class="bold-medium">Invalid credentials</h1>
    <p>Your Email and/or code were not recognised</p>
</div>
<% } %>

<div class="grid-row">
    <div class="col-8">
        <h1 class="heading-xlarge">
            Enter your verification code
        </h1>
        <p class="lede">Enter the verification code included in the email we sent you.</p>
    </div>
</div>
<div class="grid-row">
    <div class="col-12">
        <form method="post">
            <input type="hidden" name="_csrf" value="<%=csrfToken%>" />
            <div class="form-group <%= locals.validationMessages.password ? 'form-group-error' : '' %>">
                <label class="form-label" for="code">Verification code</label>
                <% if (locals.validationMessages.code) { %>
                <p class="error-message" id="validation-code"><%=locals.validationMessages.code%></p>
                <% } %>
                <div>
                    <input class="form-control char-width-8" id="code" name="code" type="text" required="required"
                            <% if (locals.validationMessages.code) { %> aria-invalid="true" aria-describedby="validation-code" <% } %>>
                </div>
            </div>
            <div class="form-group">
                <input type="hidden" name="id" id="invitationId" value="<%=locals.id%>">
                <input type="submit" class="button" value="Submit">
            </div>
        </form>
    </div>
</div>
<div class="grid-row">
    <div class="col-8">
        <%- include('../../includes/helpdesk'); %>
    </div>
</div>